{
	"name": "redirect-output",
	"version": "0.0.1",
	"author": "Abashkin Alexander <monolithed@gmail.com>",
	"license": "MIT",

	"description": "Provides a way to redirect your process.stdout to a file",
	"keywords": [
		"console.file",
		"console.log",
		"process.stdout",
		"log",
		"output",
		"logger",
		"stdout"
	],

	"engines": {
		"node": ">=7"
	},

	"repository": {
		"type": "git",
		"url": "https://github.com/monolithed/redirect-output.git"
	},

	"scripts": {
		"test": "npm prune && tsc && DEBUG=redirect-output mocha tests",
		"prepublishOnly": "npm test"
	},

	"types": "index.d.ts",

	"dependencies": {
		"@types/node": "^8.0.30",
		"@types/mkdirp": "^0.5.1",
		"@types/debug": "^0.0.30",
		"debug": "^3.0.1",
		"mkdirp": "^0.5.1"
	},

	"devDependencies": {
		"@types/mocha": "^2.2.43",
		"@types/rimraf": "^2.0.2",
		"mocha": "^3.5.3",
		"rimraf": "^2.6.2",
		"typescript": "^2.5.2"
	}
}
